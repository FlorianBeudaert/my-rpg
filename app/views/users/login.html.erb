<h1 class="form-title">Login</h1>

<%= form_with url: login_path, local: true, html: { class: "form" } do |form| %>
  <%= form.label :email %>
  <%= form.text_field :email %>

  <%= form.label :password %>
  <%= form.password_field :password %>

  <%= form.submit "Login" %>
<% end %>